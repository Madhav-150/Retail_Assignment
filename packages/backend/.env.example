# Database Configuration
# Connect to a PostgreSQL database (e.g. Neon, Supabase, Railway, Render)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Application Settings
PORT=3000
NODE_ENV="development"

# Security (Replace with long random strings in production)
JWT_SECRET="super-secret-jwt-key-change-this"
SESSION_SECRET="super-secret-session-key-change-this"

# Client URL (for CORS)
CLIENT_URL="http://localhost:5173"
